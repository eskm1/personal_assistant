# Pinned to exact installed versions — the auto-deploy timer runs pip install on
# every push, so loose pins risk an unattended breaking upgrade. Bump deliberately.

# Telegram bot ([job-queue] adds APScheduler for Ava's daily morning push)
python-telegram-bot[job-queue]==21.9

# LLM
anthropic==0.104.1

# Voice transcription
openai==2.38.0

# Config
python-dotenv==1.2.2

# Gmail
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
google-api-python-client==2.196.0

# Microsoft Graph
msal==1.36.0

# HTTP (used directly by Graph + umcpm tools)
requests==2.34.2

# Web browsing tool (HTML parsing)
beautifulsoup4==4.15.0

# Timezone database — ZoneInfo("Asia/Singapore") needs this where the OS ships no tzdata
tzdata==2026.2

# Phase 7: Telethon (Telegram DMs as yourself) — not yet enabled
# telethon==1.36.0
